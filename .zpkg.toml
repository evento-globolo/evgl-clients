[package]
org = "evento-globolo"
name = "evgl-clients"
version = "0.1.0"
description = "Polyglot SDK clients for Evento Globolo"
license = "MIT"
language = "polyglot"

[package.repository]
vcs = "git"
url = "https://github.com/evento-globolo/evgl-clients"

[install]
dir = ".vendor/.zed"

[dependencies]
"evento-globolo/evgl-interfaces" = "^0.1.0"
"evento-globolo/evgl-libs" = "^0.1.0"

[publish]
include_readme = true
tag_format = "v{version}"
exclude = [".env", ".env.*", "node_modules/**", "target/**", "dist/**", "*.log", ".vendor/.zed/**"]

[scripts]
test = "bash scripts/test.sh"
validate-client-matrix = "python3 scripts/validate-client-matrix.py"

[targets.repository]
dir = "."
name = "evgl-clients-repository"

[targets.gleam]
dir = "clients/gleam"

[targets.erlang]
dir = "clients/erlang"

[targets.elixir]
dir = "clients/elixir"

[targets.dart]
dir = "clients/dart"
adapter = "dart"

[targets.rust]
dir = "clients/rust"
adapter = "rust"

[targets.rust-wasm]
dir = "clients/wasm"
adapter = "node"

[targets.java]
dir = "clients/java"
adapter = "java"

[targets.golang]
dir = "clients/go"
adapter = "go"

[targets.python]
dir = "clients/python"
adapter = "python"

[targets.ruby]
dir = "clients/ruby"

[targets.php]
dir = "clients/php"

[targets.nodejs]
dir = "clients/typescript/nodejs"
adapter = "node"

[targets.deno]
dir = "clients/typescript/deno"

[targets.bun]
dir = "clients/typescript/bun"
adapter = "node"

[targets.edge]
dir = "clients/typescript/edge"
