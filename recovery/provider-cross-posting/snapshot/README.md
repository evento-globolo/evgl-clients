# evgl-clients

Typed Evento Globolo clients for browser, server, mobile, and automation use.

Rust and TypeScript clients implement provider discovery, OAuth connection start,
canonical event creation, idempotent cross-posting, job retrieval, and WebSocket URL
construction. Go and Dart provide the same authentication and health foundation and
are ready for generated expansion from `evgl-interfaces`.
